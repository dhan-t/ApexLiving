// constants/icons.ts
import { Home, Settings, User, Contact, Map } from "lucide-react-native";

export default {
  home: Home,
  settings: Settings,
  profile: User,
  contacts: Contact,
  map: Map,
};
